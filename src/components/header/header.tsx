import logo from '@/assets/logo.png'

const Header = () => {
    return(
        <header>
            <nav>
                <img src={logo}/>
            </nav>
        </header>
    )
}

export default Header